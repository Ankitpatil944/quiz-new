# API Configuration
# For production with CORS issues, use the proxy:
# VITE_API_BASE_URL=/api/quiz
# For direct API access (requires CORS configuration on backend):
VITE_API_BASE_URL=https://zettanix.in

# Development Configuration
VITE_DEV_MODE=true
VITE_ENABLE_LOGGING=true 